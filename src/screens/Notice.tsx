import React from 'react';
import { ScrollView, Text } from 'react-native';

const Notice = () => {
    return (
        <ScrollView>
            <Text>Notice</Text>
        </ScrollView>
    );
};

export default Notice;
